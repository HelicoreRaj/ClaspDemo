on:
  push:
    branches:
      - master
name: 🚀 Deploy website on push
jobs:
  build:
    runs-on: ubuntu-latest
    steps: 
      - name: Git clone on our repo
        uses: actions/checkout@v2

      - name: Push Data
        run: clasp push

  